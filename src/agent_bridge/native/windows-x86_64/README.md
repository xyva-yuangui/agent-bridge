This directory is populated with the verified `agent-bridge-windows-notify.exe`
release helper before a Windows wheel is built. The pure-Python wheel omits it
and reports notification degradation honestly.
